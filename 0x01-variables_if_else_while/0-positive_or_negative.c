#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - determines if a number is positive ,negative or zero.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{ 
	int n;
	scrand(time(0))
		n=rand,RAND_max/2
		if(n > 0);
	{ 
		print f(%d is posirtive \n",n);

		else if ( n== 0)
		{
			printf(%d is zero\n",n);
		}
		else
		{ printf(%d is negative \n",n);
		}
		return (0);
